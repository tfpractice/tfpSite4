<% category = @category %>



<div class="categoryDiv">

	<ul class="categoryList" id="<%= category.name.downcase %>+List">
				<h1>	<%=category.name.upcase %></h1>
				<p class = "categoryDescription" id = "<%= category.name + "Description" %>"> <%= category.description %> </p>
					<span class="projectList" id = "<%= category.name.downcase + "ProjectList"%>">









						
							<%category.projects.nonDev.each do |project| %>
												<%# subject.projects.portfolio.uniq.each do |project| %>
												<% @project= project %>
												<%= link_to (image_tag(project.linkfilepath, class: "projectIcon", size: "80x80")), project_path(@project, format: :js), remote: true, class: "projectLink" %>
												
						
						
												<%#end%>
												<%end%>
											
					</span>
					</ul>
							


						<%= render 'portfolio/devSkills', object: @category %>




<!--
			<div class = "skillsDiv">
					<ul class = "skillsList" >

						<% category.skills.each do |skill| %>


						<li> <%= skill.name %> </li>
						<% end %>

										</ul>

						</div>

-->



	</div>